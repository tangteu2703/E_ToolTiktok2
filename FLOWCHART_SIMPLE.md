# Flowchart Đơn Giản - E_ToolTiktok

## 🎯 Luồng Chính - Đăng Ký 1 Tài Khoản

```
                    ┌─────────────────┐
                    │   USER INPUT     │
                    │  - Country: VN   │
                    │  - Password: *** │
                    │  - UseProxy: ✓   │
                    └────────┬──────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  API Controller │
                    │  Validate Input │
                    └────────┬──────────┘
                             │
                             ▼
        ┌─────────────────────────────────────┐
        │  TiktokRegistrationService          │
        │  RegisterAccountAsync()            │
        └────────┬────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                │
        ▼                ▼
┌───────────────┐  ┌───────────────┐
│ Step 1:       │  │ Step 2:       │
│ Get Country   │  │ Create Email  │
│ Info          │  │ (if empty)    │
│               │  │               │
│ country =     │  │ email =       │
│ Countries     │  │ inboxes.com   │
│ .GetByCode()  │  │ .Create()     │
└───────────────┘  └───────────────┘
        │                │
        └────────┬────────┘
                 │
        ┌────────┴────────┐
        │                │
        ▼                ▼
┌───────────────┐  ┌───────────────┐
│ Step 3:       │  │ Step 4:       │
│ Generate      │  │ Get Proxy     │
│ Username      │  │ (if UseProxy) │
│               │  │               │
│ username =    │  │ proxy =       │
│ NameGen       │  │ ProxyService  │
│ .Generate()   │  │ .GetProxy()   │
└───────────────┘  └───────────────┘
        │                │
        └────────┬────────┘
                 │
                 ▼
        ┌─────────────────┐
        │ Step 5:          │
        │ Register TikTok  │
        │                  │
        │ TiktokApiService │
        │ .Register()      │
        └────────┬──────────┘
                 │
        ┌────────┴────────┐
        │                │
        ▼                ▼
┌───────────────┐  ┌───────────────┐
│ Success?      │  │ Failed?       │
│               │  │               │
│ Yes           │  │ Return Error │
└───────┬───────┘  └───────────────┘
        │
        ▼
┌─────────────────┐
│ Step 6:         │
│ Verify Email?   │
│                 │
│ if (!Verified)  │
└────────┬─────────┘
         │
    ┌────┴────┐
    │        │
    ▼        ▼
┌────────┐ ┌────────┐
│ Yes    │ │ No     │
│        │ │        │
│ Get    │ │ Return │
│ Code   │ │ Result │
│ from   │ │        │
│ Email  │ │        │
└───┬────┘ └────────┘
    │
    ▼
┌───────────────┐
│ Step 7:       │
│ Verify Email  │
│               │
│ Call TikTok   │
│ Verify API    │
└───────┬───────┘
        │
        ▼
┌─────────────────┐
│ Return Result   │
│ - Success       │
│ - Account Info  │
│ - Verified      │
└─────────────────┘
```

## 🔄 Luồng Auto Registration (5 Accounts)

```
┌─────────────────────────────────────────────────────────────┐
│  User: Create 5 accounts, Country: VN, UseProxy: true      │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  JavaScript Loop (5 times)    │
        │  for (i = 0; i < 5; i++)      │
        └───────────────┬─────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌───────────────┐              ┌───────────────┐
│ Account 1    │              │ Account 2    │
│              │              │              │
│ Email:       │              │ Email:       │
│ user1@...    │              │ user2@...    │
│              │              │              │
│ Username:    │              │ Username:    │
│ nguyenvana   │              │ tranvanb     │
│              │              │              │
│ Proxy:       │              │ Proxy:       │
│ 14.123...    │              │ 27.234...    │ ← Khác nhau
│              │              │              │
│ ──────────── │              │ ──────────── │
│              │              │              │
│ Register...  │              │ Register...  │
│ Verify...    │              │ Verify...    │
│              │              │              │
│ Success ✓    │              │ Success ✓    │
└───────────────┘              └───────────────┘
        │                               │
        │ Wait 3 seconds                │
        │                               │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  ... (Account 3, 4, 5) ...    │
        └───────────────────────────────┘
```

## 🔧 Chi Tiết TiktokApiService.RegisterWithApiAsync()

```
RegisterWithApiAsync(request)
│
├─► [1] Create HttpClient với Proxy
│   │
│   ├─► if (UseProxy && ProxyAddress != null)
│   │   │
│   │   ├─► Create HttpClientHandler
│   │   │   └─► Set Proxy: WebProxy(ProxyAddress:Port)
│   │   │
│   │   └─► Create HttpClient với Handler
│   │
│   └─► else
│       └─► Create HttpClient không proxy
│
├─► [2] Set Headers theo Country
│   │
│   ├─► Accept-Language: country.Language
│   ├─► User-Agent: Chrome/120...
│   └─► Referer: https://www.tiktok.com
│
├─► [3] Get CSRF Token
│   │
│   ├─► GET /signup
│   ├─► Parse HTML
│   │   ├─► Pattern 1: csrf_token="..."
│   │   ├─► Pattern 2: window.__UNIVERSAL_DATA...
│   │   └─► Pattern 3: Cookies
│   │
│   └─► Save: _csrfToken = "abc123..."
│
├─► [4] Create Payload
│   │
│   ├─► email: request.Email
│   ├─► password: request.Password
│   ├─► username: request.Username
│   ├─► region: country.Region ("VN")
│   ├─► tz_name: country.TimeZone ("Asia/Ho_Chi_Minh")
│   ├─► app_language: country.Language ("vi")
│   ├─► verifyFp: GenerateVerifyFp()
│   └─► device_id: GenerateDeviceId()
│
├─► [5] Try Multiple Endpoints
│   │
│   ├─► Endpoint 1: /passport/web/account/register/
│   │   ├─► POST với payload
│   │   ├─► Check response.status_code
│   │   └─► if (status_code == 0) → Success
│   │
│   ├─► Endpoint 2: /passport/web/email/register/
│   │   └─► (tương tự)
│   │
│   └─► ... (8 endpoints total)
│
└─► [6] Return Result
    │
    ├─► if Success
    │   └─► RegistrationResponse {
    │         Success: true,
    │         Account: TiktokAccount {...}
    │       }
    │
    └─► if Failed
        └─► RegistrationResponse {
              Success: false,
              Message: "Error message",
              ErrorCode: "API_CONNECTION_FAILED"
            }
```

## 📧 Chi Tiết Email Verification

```
GetVerificationCodeAsync(email, timeoutSeconds: 120)
│
├─► [1] Poll Inboxes.com API
│   │
│   ├─► Start: time = 0
│   │
│   ├─► Loop: while (time < timeoutSeconds)
│   │   │
│   │   ├─► GET /api/messages/{email}
│   │   │
│   │   ├─► Parse response
│   │   │   └─► messages = [...]
│   │   │
│   │   ├─► foreach (message in messages)
│   │   │   │
│   │   │   ├─► if (message.from contains "tiktok")
│   │   │   │   │
│   │   │   │   └─► Extract code từ message.body
│   │   │   │       ├─► Regex: \b\d{6}\b
│   │   │   │       └─► Return: "123456"
│   │   │   │
│   │   │   └─► else
│   │   │       └─► Continue
│   │   │
│   │   ├─► Wait 2 seconds
│   │   │
│   │   └─► time += 2
│   │
│   └─► Timeout → Return null
│
└─► [2] Verify với TikTok
    │
    ├─► POST /passport/web/email/verify/
    │   Body: {
    │     email: "abc@mail.com",
    │     code: "123456"
    │   }
    │
    └─► Return: true/false
```

## 🎨 Visual Flow - Complete Process

```
┌─────────────────────────────────────────────────────────────────┐
│                         START                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │ User Input     │
                    │ Country: VN    │
                    │ Password: ***  │
                    └────────┬───────┘
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │  TiktokRegistrationService              │
        │  RegisterAccountAsync()                │
        └────────┬────────────────────────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌────────┐  ┌────────┐  ┌────────┐
│Country │  │ Email  │  │Username│
│Info    │  │Create  │  │Generate│
└───┬────┘  └───┬────┘  └───┬────┘
    │            │            │
    └────────────┼────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ Get Proxy      │
        │ (if UseProxy)  │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │ Register TikTok│
        │ via API        │
        └────────┬───────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    ┌────────┐      ┌────────┐
    │Success │      │ Failed │
    └───┬────┘      └────────┘
        │
        ▼
┌────────────────┐
│ Need Verify?   │
└────────┬───────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────┐
│ Yes   │ │ No    │
│       │ │       │
│ Get   │ │ Return│
│ Code  │ │ Result│
└───┬───┘ └───────┘
    │
    ▼
┌───────────────┐
│ Verify Email  │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│   SUCCESS     │
│ Account Info  │
│ Saved to File │
└───────────────┘
```

## 📊 Data Flow

```
Request Data:
┌─────────────────────────────────────┐
│ RegistrationRequest                 │
│ {                                   │
│   CountryCode: "VN"                 │
│   Email: "" (empty)                 │
│   Password: "mypassword123"         │
│   Username: "" (empty)              │
│   UseProxy: true                     │
│ }                                   │
└─────────────────────────────────────┘
              │
              ▼ Process
┌─────────────────────────────────────┐
│ After Processing:                   │
│ {                                   │
│   CountryCode: "VN"                 │
│   Email: "abc123@vomoto.com"         │ ← Created
│   Password: "mypassword123"         │
│   Username: "nguyenvana1234"        │ ← Generated
│   UseProxy: true                     │
│   ProxyAddress: "14.123.45.67"      │ ← Assigned
│   ProxyPort: 8080                    │ ← Assigned
│   ProxyType: "http"                  │ ← Assigned
│ }                                   │
└─────────────────────────────────────┘
              │
              ▼ Send to TikTok
┌─────────────────────────────────────┐
│ TikTok API Payload                  │
│ {                                   │
│   email: "abc123@vomoto.com"       │
│   password: "mypassword123"         │
│   username: "nguyenvana1234"        │
│   region: "VN"                      │ ← From Country
│   tz_name: "Asia/Ho_Chi_Minh"      │ ← From Country
│   app_language: "vi"                │ ← From Country
│   browser_language: "vi"            │ ← From Country
│   verifyFp: "abc123def456..."       │ ← Generated
│   device_id: "1234567890abcdef"     │ ← Generated
│   ...                               │
│ }                                   │
└─────────────────────────────────────┘
```

## 🔑 Key Points

1. **Mỗi Account = 1 Proxy Khác Nhau**
   - ProxyService.GetProxyAsync() được gọi mỗi lần
   - Cache proxy để tái sử dụng nhưng mỗi account vẫn khác nhau

2. **Tất Cả Thông Tin Theo Country**
   - Username: Tên theo quốc gia
   - Region: Country code
   - TimeZone: Theo country
   - Language: Theo country
   - Proxy: IP từ country đó

3. **Tự Động Hóa Hoàn Toàn**
   - Tạo email tự động
   - Tạo username tự động
   - Lấy proxy tự động
   - Verify email tự động

4. **Error Handling**
   - Nếu proxy fail → Thử proxy khác
   - Nếu endpoint fail → Thử endpoint khác
   - Nếu verify fail → Trả về code để verify thủ công

